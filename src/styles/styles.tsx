import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    btn: {
        backgroundColor: "#114",
        margin: 5,
        borderRadius: 20
    },
    btnText: {
        fontSize: 20,
        textAlign: "center",
        color: "#fff",
        padding: 10
    }
})