import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    btn: {
        backgroundColor: "#114",
        margin: 5,
        borderRadius: 120
    },
    btnText: {
        fontSize: 20,
        textAlign: "center",
        color: "#fff",
        padding: 10
    }
})
export default styles;